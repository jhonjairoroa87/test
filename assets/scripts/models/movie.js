define([
  'underscore',
  'backbone',
  'scripts/models/base'
], function(_, Backbone, BaseModel){
  var MovieModel = BaseModel.extend({});
  // Return the model for the module
  return MovieModel;
});

