define([
  'underscore',
  'backbone',
  'scripts/models/base'
], function(_, Backbone, BaseModel){
  var ActorModel = BaseModel.extend({});
  // Return the model for the module
  return ActorModel;
});

